# План реализации функционала активации контроллера без персональных данных

## Обзор

Данный документ описывает совместный план доработки функционала активации контроллера и регистрации пользователя без обработки персональных данных (ФЗ-152) для проектов `esp32` и `server`.

**Цель:** Реализовать систему активации контроллеров с физическим подтверждением доступа, где пользователи работают через анонимные кабинеты без использования email/паролей.

---

## Текущее состояние проектов

### Server (Backend + Frontend)

**Реализовано:**
- ✅ Система авторизации через email/password (JWT токены)
- ✅ WebSocket туннель для подключения контроллеров
- ✅ API для управления контроллерами (`/api/controllers/*`)
- ✅ Система активации через `activation_token`
- ✅ Frontend на Next.js с авторизацией
- ✅ База данных PostgreSQL с таблицами: `users`, `controllers`, `controller_sessions`

**Требуется изменить:**
- ❌ Убрать зависимость от email/password (для обычных пользователей)
- ❌ Заменить таблицу `users` на `cabinets`
- ❌ Реализовать систему физического подтверждения доступа
- ❌ Добавить API для активации через коды подтверждения
- ❌ Реализовать систему авторизации устройств через Ed25519
- ❌ Добавить систему суперадмина (username/password для технической поддержки)

### ESP32 (Controller)

**Реализовано:**
- ✅ WiFi подключение (Station mode)
- ✅ Веб-сервер на порту 80 (AsyncWebServer)
- ✅ Локальный веб-интерфейс для настройки
- ✅ Работа с майнерами и датчиками
- ✅ Сохранение настроек в Preferences

**Требуется добавить:**
- ❌ WiFi AP режим для первоначальной настройки
- ❌ Генерация и отображение activation_code
- ❌ Страница активации в веб-интерфейсе
- ❌ WebSocket клиент для подключения к серверу
- ❌ Обработка кодов подтверждения
- ❌ Сохранение cabinet_secret и controller_secret

---

## Архитектура новой системы

### Схема базы данных

```sql
-- Кабинеты (заменяет users)
CREATE TABLE cabinets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  cabinet_secret_hash VARCHAR(255) UNIQUE NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  last_activity TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Контроллеры (изменена структура)
CREATE TABLE controllers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  cabinet_id UUID REFERENCES cabinets(id) ON DELETE CASCADE,
  mac_address VARCHAR(17) UNIQUE NOT NULL,
  controller_secret_hash VARCHAR(255) UNIQUE NOT NULL,
  firmware_version VARCHAR(50),
  is_active BOOLEAN DEFAULT false,
  last_seen_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Ожидающие активации (новая таблица)
CREATE TABLE pending_activations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  activation_code VARCHAR(12) UNIQUE NOT NULL,
  cabinet_id UUID REFERENCES cabinets(id) ON DELETE CASCADE,
  device_authorization_code VARCHAR(6) NOT NULL,
  controller_mac VARCHAR(17),
  expires_at TIMESTAMP NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Запросы на доступ к кабинету (новая таблица)
CREATE TABLE pending_access_requests (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  cabinet_id UUID REFERENCES cabinets(id) ON DELETE CASCADE,
  access_request_code VARCHAR(6) UNIQUE NOT NULL,
  session_token VARCHAR(255) UNIQUE,
  expires_at TIMESTAMP NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  confirmed_at TIMESTAMP,
  confirmed_by_controller_id UUID REFERENCES controllers(id)
);

-- Авторизованные устройства (новая таблица)
CREATE TABLE authorized_devices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  cabinet_id UUID REFERENCES cabinets(id) ON DELETE CASCADE,
  device_fingerprint VARCHAR(255) NOT NULL,
  public_key TEXT NOT NULL,
  authorized_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  last_used_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(cabinet_id, device_fingerprint)
);

-- Суперадмины (для технической поддержки и управления)
CREATE TABLE superadmins (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  username VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  is_active BOOLEAN DEFAULT true,
  last_login_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Протокол WebSocket (обновленный)

```typescript
// Контроллер → Сервер (при подключении)
{
  type: "register",
  secret: "controller_secret",  // Вместо token/mac
  firmwareVersion?: string
}

// Сервер → Контроллер
{
  type: "registered",
  controllerId: "uuid",
  status: "active"
}
```

---

## Этапы реализации

### Этап 1: Подготовка инфраструктуры (Server) ✅ ЗАВЕРШЕН

**Приоритет:** Высокий  
**Зависимости:** Нет  
**Оценка:** 2-3 дня  
**Фактическое время:** 1 день  
**Статус:** ✅ Завершен  
**Коммиты:** `a2bd849`, `74897df`

#### 1.1. Миграция базы данных ✅

**Файл:** `server/src/database/migrations/002_cabinet_system.sql`

**Задачи:**
- [x] Создать новую миграцию для таблиц `cabinets`, `pending_activations`, `pending_access_requests`, `authorized_devices`
- [x] Обновить таблицу `controllers` (добавить `controller_secret_hash`, изменить `user_id` → `cabinet_id`)
- [x] Создать индексы для оптимизации запросов
- [x] Добавить функции для работы с хешами секретов

**Критерии готовности:**
- ✅ Миграция успешно применяется
- ✅ Все индексы созданы
- ✅ Старые данные могут быть мигрированы (если нужно)

**Результат:** Миграция применена и проверена. Все таблицы созданы, индексы и триггеры работают.

**Созданные файлы:**
- `src/database/migrations/002_cabinet_system.sql` - миграция БД
- `scripts/check-migration.ts` - скрипт проверки миграции

#### 1.2. Утилиты для работы с секретами ✅

**Файл:** `server/src/utils/crypto.ts` (новый)

**Задачи:**
- [x] Функция генерации `cabinet_secret` (32 байта, base64url)
- [x] Функция генерации `controller_secret` (32 байта, base64url)
- [x] Функция хеширования секретов (SHA-256)
- [x] Функция генерации кодов подтверждения (6 цифр)
- [x] Функция генерации activation_code (12 символов)
- [x] Функция генерации session_token

**Критерии готовности:**
- ✅ Все функции протестированы
- ✅ Секреты генерируются криптографически стойко
- ✅ Хеширование работает корректно

**Результат:** Все функции реализованы и протестированы. Тестовый скрипт `scripts/test-crypto-utils.ts` подтверждает работоспособность.

**Созданные файлы:**
- `src/utils/crypto.ts` - утилиты для работы с секретами
- `scripts/test-crypto-utils.ts` - тестовый скрипт для проверки утилит

#### 1.3. Утилиты для Ed25519 ✅

**Файл:** `server/src/utils/ed25519.ts` (новый)

**Задачи:**
- [x] Установка библиотеки для Ed25519 (`@noble/ed25519`, `@noble/hashes`)
- [x] Настройка SHA-512 для Ed25519
- [x] Функция проверки подписи
- [x] Функция генерации device_fingerprint
- [x] Функция создания подписи (для тестирования)
- [x] Функция форматирования публичного ключа

**Критерии готовности:**
- ✅ Библиотека установлена
- ✅ Функции работают корректно

**Результат:** Все функции реализованы и протестированы. Исправлена проблема с настройкой SHA-512 (используется `hashes.sha512` вместо `etc.sha512Sync`).

**Созданные файлы:**
- `src/utils/ed25519.ts` - утилиты для Ed25519 подписей

**Примечание:** Проблема с настройкой SHA-512 была решена путем использования правильного API библиотеки `@noble/ed25519` - настройка через `hashes.sha512` и `hashes.sha512Async`. Подробности в разделе "Известные проблемы и решения".

**Установленные зависимости:**
- `@noble/ed25519` - библиотека для Ed25519 подписей
- `@noble/hashes` - библиотека для хеш-функций (SHA-512)

#### 1.4. Таблица суперадминов ✅

**Файл:** `server/src/database/migrations/002_cabinet_system.sql`

**Задачи:**
- [x] Создать таблицу `superadmins` с полями: id, username, password_hash, is_active, last_login_at
- [x] Создать индекс на username
- [x] Добавить триггер для updated_at
- [ ] Создать начального суперадмина через seed скрипт (опционально, будет реализовано в Этапе 3)

**Критерии готовности:**
- ✅ Таблица создана
- ✅ Индексы созданы
- ✅ Можно создать суперадмина через API (будет реализовано в Этапе 3)

---

### Этап 2: API активации (Server)

**Приоритет:** Высокий  
**Зависимости:** Этап 1  
**Оценка:** 3-4 дня

#### 2.1. API инициации активации

**Файл:** `server/src/api/routes/activation.ts` (новый)

**Эндпоинт:** `POST /api/activation/initiate`

**Задачи:**
- [x] Валидация входных данных (activation_code, mac_address, action)
- [x] Проверка существования activation_code на контроллере
- [x] Логика создания нового кабинета или добавления в существующий
- [x] Генерация device_authorization_code
- [x] Сохранение в `pending_activations`
- [x] Rate limiting для предотвращения перебора

**Критерии готовности:**
- ✅ Эндпоинт работает корректно
- ✅ Валидация данных работает
- ✅ Rate limiting настроен
- ✅ Тесты написаны

#### 2.2. API подтверждения активации на контроллере

**Файл:** `server/src/api/routes/controllers.ts`

**Эндпоинт:** `POST /api/controllers/confirm-activation`

**Задачи:**
- [x] Валидация activation_code и device_authorization_code
- [x] Проверка срока действия кода
- [x] Создание записи контроллера
- [x] Генерация controller_secret
- [x] Возврат cabinet_secret (только при создании нового кабинета)
- [x] Удаление записи из pending_activations

**Критерии готовности:**
- ✅ Эндпоинт работает корректно
- ✅ Контроллер создается в БД
- ✅ Секреты генерируются и возвращаются
- ✅ Тесты проходят успешно

#### 2.3. Обновление WebSocket регистрации

**Файл:** `server/src/tunnel/TunnelService.ts`

**Задачи:**
- [x] Изменить логику регистрации: использовать `controller_secret` вместо `token`/`mac`
- [x] Проверка `controller_secret_hash` в БД
- [x] Обновить интерфейс `RegisterMessage`
- [x] Обновить логику активации контроллера
- [x] Обновить `TunnelConnection`: заменить `userId` на `cabinet_id`
- [x] Создать тестовый скрипт для проверки WebSocket регистрации

**Критерии готовности:**
- ✅ WebSocket регистрация работает с новым протоколом
- ✅ Контроллеры подключаются успешно
- ✅ Тесты проходят успешно
- ✅ Запросы с неправильным секретом корректно отклоняются

---

### Этап 3: API доступа к кабинету (Server)

**Приоритет:** Высокий  
**Зависимости:** Этап 2  
**Оценка:** 3-4 дня

#### 3.1. API запроса доступа к кабинету

**Файл:** `server/src/api/routes/cabinets.ts` (новый)

**Эндпоинт:** `POST /api/cabinets/request-access`

**Задачи:**
- [x] Валидация cabinet_id
- [x] Генерация access_request_code
- [x] Сохранение в pending_access_requests
- [x] Rate limiting
- [x] Создание тестового скрипта

**Критерии готовности:**
- ✅ Эндпоинт работает
- ✅ Коды генерируются корректно
- ✅ Валидация работает (UUID формат, существование кабинета)
- ✅ Rate limiting работает (10 запросов за 15 минут)
- ✅ Тесты проходят успешно

#### 3.2. API подтверждения доступа на контроллере

**Файл:** `server/src/api/routes/cabinets.ts`

**Эндпоинт:** `POST /api/cabinets/confirm-access`

**Задачи:**
- [x] Авторизация через controller_secret
- [x] Проверка access_request_code
- [x] Генерация session_token
- [x] Возврат session_token и cabinet_id (cabinet_secret не хранится в БД, пользователь использует сохраненный)
- [x] Обновление pending_access_requests
- [x] Создание тестового скрипта

**Критерии готовности:**
- ✅ Эндпоинт работает
- ✅ Авторизация контроллера работает
- ✅ Проверка access_request_code работает
- ✅ Повторное использование кода отклоняется
- ✅ Тесты проходят успешно

#### 3.3. API авторизации устройства

**Файл:** `server/src/api/routes/cabinets.ts`

**Эндпоинт:** `POST /api/cabinets/authorize-device`

**Задачи:**
- [x] Валидация session_token
- [x] Проверка срока действия
- [x] Сохранение public_key и device_fingerprint
- [x] Создание записи в authorized_devices
- [x] Поддержка повторной авторизации (обновление last_used_at)
- [x] Создание тестового скрипта

**Критерии готовности:**
- ✅ Эндпоинт работает
- ✅ Устройства авторизуются корректно
- ✅ Повторная авторизация обновляет last_used_at
- ✅ Проверка session_token работает
- ✅ Тесты проходят успешно

#### 3.4. API входа в кабинет

**Файл:** `server/src/api/routes/cabinets.ts`

**Эндпоинт:** `POST /api/cabinets/access`

**Задачи:**
- [x] Проверка cabinet_secret из заголовка Authorization
- [x] Проверка подписи Ed25519
- [x] Проверка device_fingerprint
- [x] Генерация JWT токена для доступа к API
- [x] Обновление last_used_at
- [x] Создание тестового скрипта

**Критерии готовности:**
- ✅ Эндпоинт работает
- ✅ Авторизация через Ed25519 работает
- ✅ Проверка cabinet_secret работает
- ✅ Проверка device_fingerprint работает
- ✅ JWT токен генерируется корректно
- ✅ Тесты проходят успешно

#### 3.5. Middleware для авторизации кабинетов

**Файл:** `server/src/middleware/cabinetAuth.ts` (новый)

**Задачи:**
- [x] Проверка JWT токена кабинета
- [x] Проверка прав доступа к контроллерам
- [x] Интеграция с существующими роутами
- [x] Поддержка обратной совместимости (старая система users)

**Критерии готовности:**
- ✅ Middleware работает
- ✅ Защита роутов работает
- ✅ Поддержка обоих типов авторизации (users и cabinets)
- ✅ Код компилируется успешно

#### 3.6. API суперадмина

**Файл:** `server/src/api/routes/superadmin.ts` (новый)

**Эндпоинты:**
- `POST /api/superadmin/login` - Авторизация суперадмина
- `GET /api/superadmin/profile` - Получение профиля
- `PUT /api/superadmin/change-credentials` - Смена логина/пароля
- `GET /api/superadmin/cabinets` - Список всех кабинетов
- `GET /api/superadmin/cabinets/:id` - Детали кабинета
- `GET /api/superadmin/controllers` - Список всех контроллеров
- `GET /api/superadmin/controllers/:id` - Детали контроллера
- `POST /api/superadmin/controllers/:id/reset` - Сброс контроллера (отвязка от кабинета)
- `DELETE /api/superadmin/cabinets/:id` - Удаление кабинета (с контроллерами)

**Задачи:**
- [x] Реализация авторизации через username/password
- [x] Генерация JWT токена для суперадмина
- [x] Middleware для проверки прав суперадмина (superadminAuth.ts)
- [x] API для просмотра всех кабинетов и контроллеров
- [x] API для управления кабинетами и контроллерами
- [x] API для смены логина/пароля
- [x] Логирование действий суперадмина
- [x] Скрипт для создания первого суперадмина

**Критерии готовности:**
- ✅ Все эндпоинты работают
- ✅ Авторизация работает
- ✅ Права проверяются корректно
- ✅ Логирование работает
- ✅ Код компилируется успешно

#### 3.7. Middleware для суперадмина

**Файл:** `server/src/middleware/superadminAuth.ts` (новый)

**Задачи:**
- [x] Проверка JWT токена суперадмина
- [x] Проверка активности суперадмина
- [x] Добавление информации о суперадмине в request

**Критерии готовности:**
- ✅ Middleware работает
- ✅ Защита роутов работает
- ✅ Реализовано в рамках этапа 3.6

---

### Этап 4: Frontend (Server)

**Приоритет:** Высокий  
**Зависимости:** Этап 3  
**Оценка:** 4-5 дней

#### 4.1. Страница активации контроллера

**Файл:** `server/frontend/app/activate/page.tsx` (новый)

**Задачи:**
- [x] Форма выбора: "Создать кабинет" / "Добавить в существующий"
- [x] Ввод activation_code и mac_address
- [x] Ввод cabinet_secret (если добавление)
- [x] Отображение device_authorization_code
- [x] Инструкции для пользователя
- [x] Сохранение cabinet_secret в localStorage
- [x] Обновление API клиента

**Критерии готовности:**
- ✅ Страница работает
- ✅ UI/UX понятный
- ✅ Интеграция с API работает
- ✅ Код компилируется успешно

#### 4.2. Страница доступа к кабинету

**Файл:** `server/frontend/app/cabinet/[id]/page.tsx` (новый)

**Задачи:**
- [x] Проверка наличия cabinet_secret в localStorage
- [x] Если нет → показ формы запроса доступа
- [x] Отображение кода для подтверждения на контроллере
- [x] Авторизация устройства через Ed25519
- [x] Сохранение cabinet_secret в localStorage
- [x] Создание утилит для Ed25519 (crypto.ts)

**Критерии готовности:**
- ✅ Страница работает
- ✅ Авторизация устройств работает
- ✅ Код компилируется успешно

#### 4.3. Дашборд кабинета

**Файл:** `server/frontend/app/cabinet/[id]/dashboard/page.tsx` (новый)

**Задачи:**
- [x] Список контроллеров кабинета
- [x] Статус контроллеров
- [x] Управление контроллерами (удаление)
- [x] Управление авторизованными устройствами (секция создана, данные пока не загружаются)
- [x] Экспорт/импорт cabinet_secret
- [x] Копирование cabinet_secret в буфер обмена
- [x] Обновление API клиента для поддержки cabinet_access_token

**Критерии готовности:**
- ✅ Дашборд работает
- ✅ Все функции доступны
- ✅ Код компилируется успешно

#### 4.4. Обновление API клиента

**Файл:** `server/frontend/lib/api.ts`

**Задачи:**
- [x] Добавить методы для активации (initiateActivation)
- [x] Добавить методы для работы с кабинетами (requestCabinetAccess, accessCabinet)
- [x] Добавить методы для авторизации устройств (authorizeDevice)
- [x] Обновить методы работы с контроллерами (getCabinetControllers)
- [x] Добавить JSDoc комментарии для всех методов
- [x] Обновить поддержку cabinet_access_token
- [x] Пометить устаревшие методы (@deprecated)

**Критерии готовности:**
- ✅ API клиент обновлен
- ✅ Все методы работают
- ✅ Все методы задокументированы
- ✅ Код компилируется успешно

#### 4.5. Утилиты для Ed25519 (Frontend)

**Файл:** `server/frontend/lib/crypto.ts` (новый)

**Задачи:**
- [x] Генерация пары ключей Ed25519
- [x] Подпись запросов
- [x] Сохранение приватного ключа в localStorage (base64 encoded)
- [x] Генерация device_fingerprint
- [x] Проверка подписи
- [x] Установка зависимостей (@noble/ed25519, @noble/hashes)

**Критерии готовности:**
- ✅ Утилиты работают
- ✅ Безопасность обеспечена
- ✅ Код компилируется успешно
- ✅ Реализовано в рамках этапа 4.2

#### 4.6. Панель суперадмина (Frontend)

**Файл:** `server/frontend/app/superadmin/*` (новые страницы)

**Страницы:**
- `/superadmin/login` - Страница входа для суперадмина
- `/superadmin/dashboard` - Дашборд суперадмина
- `/superadmin/cabinets` - Список кабинетов
- `/superadmin/cabinets/[id]` - Детали кабинета
- `/superadmin/controllers` - Список контроллеров
- `/superadmin/controllers/[id]` - Детали контроллера
- `/superadmin/settings` - Настройки (смена логина/пароля)

**Задачи:**
- [x] Страница входа с username/password
- [x] Дашборд с общей статистикой
- [x] Список кабинетов
- [x] Детали кабинета: контроллеры, информация
- [x] Список контроллеров
- [x] Детали контроллера: статус, информация, управление
- [x] Страница настроек для смены логина/пароля
- [x] Компонент для сброса контроллера
- [x] Компонент для удаления кабинета
- [x] Все методы API добавлены в api.ts

**Критерии готовности:**
- ✅ Все страницы работают
- ✅ UI/UX понятный
- ✅ Интеграция с API работает
- ✅ Код компилируется успешно

#### 4.7. Обновление API клиента для суперадмина

**Файл:** `server/frontend/lib/api.ts`

**Задачи:**
- [ ] Добавить методы для авторизации суперадмина
- [ ] Добавить методы для работы с кабинетами (суперадмин)
- [ ] Добавить методы для работы с контроллерами (суперадмин)
- [ ] Добавить метод для смены логина/пароля

**Критерии готовности:**
- ✅ API клиент обновлен
- ✅ Все методы работают

---

### Этап 5: ESP32 - WiFi AP режим

**Приоритет:** Высокий  
**Зависимости:** Нет  
**Оценка:** 2-3 дня  
**Статус:** ✅ Завершен  
**Дата завершения:** 2024-12-19  
**Коммит:** Этап 5: Реализация WiFi AP режима для ESP32

#### 5.1. Реализация WiFi AP режима

**Файл:** `esp32/src/main.cpp`

**Задачи:**
- [x] Функция инициализации WiFi AP режима
- [x] Генерация уникального SSID (формат "WMOC-AP-XXXX" с последними 4 цифрами MAC)
- [x] Логика переключения между AP и Station режимами
- [x] Сохранение WiFi credentials в Preferences
- [x] Автоматическое переключение в Station режим после подключения
- [x] Отображение параметров AP на дисплее (SSID, пароль, IP)
- [x] Автоматическая загрузка WiFi credentials при старте
- [x] Обработка потери соединения и переподключения

**Критерии готовности:**
- ✅ AP режим работает
- ✅ Переключение режимов работает
- ✅ Credentials сохраняются и загружаются при перезагрузке
- ✅ Дисплей показывает параметры подключения в AP режиме

#### 5.2. Страница первоначальной настройки WiFi

**Файл:** `esp32/src/main.cpp` (функция `generateWiFiSetupHtml`)

**Задачи:**
- [x] HTML форма для ввода SSID и пароля WiFi
- [x] Обработка POST запроса с credentials (`/api/wifi/setup`)
- [x] Сохранение credentials в Preferences
- [x] Переключение в Station режим после настройки
- [x] Редирект на главную страницу с новым IP адресом
- [x] Редирект всех запросов на `/wifi-setup` в AP режиме
- [x] API для принудительного переключения в AP режим (`/api/wifi/switch-ap`)

**Критерии готовности:**
- ✅ Страница работает
- ✅ Подключение к WiFi работает
- ✅ Автоматический редирект после настройки WiFi
- ✅ Все страницы редиректятся на настройку WiFi в AP режиме

---

### Этап 6: ESP32 - Генерация activation_code

**Приоритет:** Высокий  
**Зависимости:** Этап 5  
**Оценка:** 1-2 дня

#### 6.1. Генерация и сохранение activation_code

**Файл:** `esp32/src/main.cpp`

**Задачи:**
- [ ] Функция генерации activation_code (12 символов: буквы и цифры)
- [ ] Сохранение activation_code в Preferences
- [ ] Проверка: если контроллер не активирован → генерировать код
- [ ] Отображение activation_code на дисплее (опционально)

**Критерии готовности:**
- ✅ Код генерируется корректно
- ✅ Код сохраняется
- ✅ Код уникален для каждого контроллера

#### 6.2. Страница активации в веб-интерфейсе

**Файл:** `esp32/src/main.cpp` (роут `/activate`)

**Задачи:**
- [ ] HTML страница с activation_code
- [ ] QR-код со ссылкой активации
- [ ] Форма для ввода device_authorization_code
- [ ] Отправка POST запроса на сервер для подтверждения
- [ ] Сохранение cabinet_secret и controller_secret

**Критерии готовности:**
- ✅ Страница работает
- ✅ QR-код генерируется
- ✅ Подтверждение работает

---

### Этап 7: ESP32 - WebSocket клиент

**Приоритет:** Высокий  
**Зависимости:** Этап 6  
**Оценка:** 3-4 дня

#### 7.1. Установка библиотеки WebSocket

**Файл:** `esp32/platformio.ini`

**Задачи:**
- [ ] Добавить библиотеку WebSocket клиента (например, `Links2004/WebSockets@^2.4.1`)
- [ ] Обновить зависимости

**Критерии готовности:**
- ✅ Библиотека установлена
- ✅ Компиляция проходит успешно

#### 7.2. Реализация WebSocket клиента

**Файл:** `esp32/src/main.cpp`

**Задачи:**
- [ ] Инициализация WebSocket клиента
- [ ] Подключение к `wss://wmoc.online/tunnel`
- [ ] Регистрация с controller_secret
- [ ] Обработка сообщений от сервера
- [ ] Обработка HTTP запросов через туннель
- [ ] Переподключение при разрыве соединения

**Критерии готовности:**
- ✅ WebSocket подключение работает
- ✅ Регистрация проходит успешно
- ✅ Туннель работает

#### 7.3. Интеграция с веб-сервером

**Файл:** `esp32/src/main.cpp`

**Задачи:**
- [ ] Обработка HTTP запросов через WebSocket туннель
- [ ] Проксирование запросов к локальному веб-серверу
- [ ] Обработка ответов и отправка через туннель

**Критерии готовности:**
- ✅ Туннель работает
- ✅ Запросы проксируются корректно

---

### Этап 8: ESP32 - Подтверждение доступа к кабинету

**Приоритет:** Средний  
**Зависимости:** Этап 7  
**Оценка:** 2-3 дня

#### 8.1. Страница подтверждения доступа

**Файл:** `esp32/src/main.cpp` (роут `/cabinet-access`)

**Задачи:**
- [ ] Отображение уведомления о запросе доступа
- [ ] Отображение access_request_code
- [ ] Кнопка "Подтвердить"
- [ ] Отправка POST запроса на сервер
- [ ] Отображение QR-кода с session_token

**Критерии готовности:**
- ✅ Страница работает
- ✅ Подтверждение работает
- ✅ QR-код генерируется

#### 8.2. Отображение ссылки на кабинет

**Файл:** `esp32/src/main.cpp` (главная страница)

**Задачи:**
- [ ] Проверка наличия cabinet_secret
- [ ] Отображение ссылки на кабинет
- [ ] QR-код со ссылкой

**Критерии готовности:**
- ✅ Ссылка отображается
- ✅ QR-код работает

---

### Этап 9: Тестирование и отладка

**Приоритет:** Высокий  
**Зависимости:** Этапы 1-8  
**Оценка:** 3-5 дней

#### 9.1. Интеграционное тестирование

**Задачи:**
- [ ] Тест полного цикла активации контроллера
- [ ] Тест создания кабинета
- [ ] Тест добавления контроллера в существующий кабинет
- [ ] Тест доступа к кабинету с нового устройства
- [ ] Тест WebSocket туннеля
- [ ] Тест работы с несколькими контроллерами
- [ ] Тест авторизации суперадмина
- [ ] Тест функционала суперадмина (просмотр кабинетов, управление контроллерами)
- [ ] Тест смены логина/пароля суперадмина

**Критерии готовности:**
- ✅ Все тесты проходят
- ✅ Нет критических багов

#### 9.2. Тестирование безопасности

**Задачи:**
- [ ] Проверка защиты от перебора кодов
- [ ] Проверка срока действия кодов
- [ ] Проверка авторизации устройств
- [ ] Проверка защиты WebSocket соединений
- [ ] Проверка защиты API суперадмина (только для авторизованных)
- [ ] Проверка защиты от перебора пароля суперадмина
- [ ] Проверка изоляции данных между кабинетами
- [ ] Проверка прав доступа суперадмина

**Критерии готовности:**
- ✅ Безопасность обеспечена
- ✅ Уязвимости устранены

#### 9.3. Тестирование производительности

**Задачи:**
- [ ] Нагрузочное тестирование API
- [ ] Тестирование WebSocket соединений
- [ ] Оптимизация запросов к БД

**Критерии готовности:**
- ✅ Производительность приемлема
- ✅ Нет утечек памяти

---

### Этап 10: Документация и миграция

**Приоритет:** Средний  
**Зависимости:** Этап 9  
**Оценка:** 2-3 дня

#### 10.1. Обновление документации

**Задачи:**
- [ ] Обновить ARCHITECTURE.md
- [ ] Создать документацию по активации
- [ ] Создать руководство пользователя
- [ ] Обновить API документацию

**Критерии готовности:**
- ✅ Документация актуальна
- ✅ Примеры работают

#### 10.2. Миграция данных (если нужно)

**Задачи:**
- [ ] Скрипт миграции существующих пользователей (если есть)
- [ ] Скрипт миграции контроллеров
- [ ] Резервное копирование данных

**Критерии готовности:**
- ✅ Миграция выполнена
- ✅ Данные сохранены

---

## Взаимозависимости этапов

```
Этап 1 (Инфраструктура)
  ↓
Этап 2 (API активации) ──┐
  ↓                      │
Этап 3 (API кабинетов) ──┤
  ↓                      │
Этап 4 (Frontend) ───────┤
                         │
Этап 5 (ESP32 AP) ───────┤
  ↓                      │
Этап 6 (ESP32 activation)┤
  ↓                      │
Этап 7 (ESP32 WebSocket)─┤
  ↓                      │
Этап 8 (ESP32 доступ) ───┤
  ↓                      │
Этап 9 (Тестирование) ←──┘
  ↓
Этап 10 (Документация)
```

---

## Критические точки интеграции

### 1. Протокол WebSocket

**Server → ESP32:**
- Формат сообщения регистрации должен совпадать
- Обработка ошибок должна быть согласована

**Решение:** Создать общий документ с протоколом сообщений

### 2. Формат activation_code

**ESP32 → Server:**
- Формат кода должен совпадать (12 символов, буквы+цифры)
- Валидация должна быть одинаковой

**Решение:** Использовать общую функцию генерации или четко описать формат

### 3. Хранение секретов

**ESP32:**
- `cabinet_secret` и `controller_secret` должны храниться безопасно
- Формат хранения должен быть согласован

**Решение:** Использовать Preferences с шифрованием (опционально)

---

## Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Несовместимость протоколов | Средняя | Высокое | Создать общий документ с протоколом, тестировать на каждом этапе |
| Потеря данных при миграции | Низкая | Высокое | Резервное копирование, тестирование миграции на копии |
| Проблемы с WebSocket на ESP32 | Средняя | Среднее | Использовать проверенную библиотеку, тестировать стабильность |
| Проблемы с производительностью | Низкая | Среднее | Нагрузочное тестирование, оптимизация запросов |
| Недостаточная безопасность | Средняя | Высокое | Code review, тестирование безопасности |

---

## Оценка времени

| Этап | Оценка (дни) | Приоритет |
|------|-------------|-----------|
| Этап 1: Инфраструктура | 2-3 | Высокий | ✅ Завершен (1 день) |
| Этап 2: API активации | 3-4 | Высокий | ✅ Завершен (3 дня) |
| Этап 3: API доступа к кабинету | 3-4 | Высокий | ✅ Завершен (1 день) |
| Этап 3: API кабинетов | 4-5 | Высокий |
| Этап 4: Frontend | 5-6 | Высокий |
| Этап 5: ESP32 AP | 2-3 | Высокий |
| Этап 6: ESP32 activation | 1-2 | Высокий |
| Этап 7: ESP32 WebSocket | 3-4 | Высокий |
| Этап 8: ESP32 доступ | 2-3 | Средний |
| Этап 9: Тестирование | 3-5 | Высокий |
| Этап 10: Документация | 2-3 | Средний |
| **ИТОГО** | **27-38 дней** | |

**Примечание:** Оценка дана для одного разработчика. При параллельной работе над разными этапами время может быть сокращено.

---

## Чеклист готовности к продакшену

- [ ] Все этапы выполнены
- [ ] Интеграционное тестирование пройдено
- [ ] Тестирование безопасности пройдено
- [ ] Документация обновлена
- [ ] Миграция данных выполнена (если нужно)
- [ ] Резервное копирование настроено
- [ ] Мониторинг настроен
- [ ] Логирование настроено
- [ ] Rate limiting настроен
- [ ] SSL/TLS сертификаты настроены
- [ ] Суперадмин создан и протестирован
- [ ] Логирование действий суперадмина работает
- [ ] Смена логина/пароля суперадмина работает

---

## Следующие шаги

1. ✅ **Согласование плана** - план утвержден
2. ✅ **Этап 1 завершен** - инфраструктура готова
3. 🔄 **Этап 2** - начало работы над API активации
4. **Еженедельные синхронизации** - проверка прогресса и решение проблем
5. **Тестирование на каждом этапе** - не переходить к следующему этапу без тестирования текущего

---

## Дополнительные замечания

### Суперадмин

**Назначение:** Техническая поддержка пользователей, управление системой, помощь в решении проблем.

**Особенности:**
- Авторизация через username/password (не нарушает ФЗ-152, так как это технический персонал)
- Полный доступ ко всем кабинетам и контроллерам
- Возможность сброса контроллеров (отвязка от кабинета)
- Возможность удаления кабинетов (в случае необходимости)
- Логирование всех действий для аудита
- Возможность смены логина/пароля для безопасности

**Безопасность:**
- Пароли хранятся в виде хешей (bcrypt)
- Rate limiting на попытки входа
- JWT токены с коротким временем жизни
- Логирование всех действий суперадмина

**Примечание:** Данные суперадмина обрабатываются для технических целей и не относятся к персональным данным пользователей системы (ФЗ-152).

---

## Статус выполнения

### Завершенные этапы

**Этап 1: Подготовка инфраструктуры (Server)** ✅
- Дата начала: 2024-12-19
- Дата завершения: 2024-12-19
- Коммиты: `a2bd849`, `74897df`
- Результат: Все задачи выполнены, миграция применена, утилиты протестированы

### Текущий этап

**Этап 4: Frontend (Server)** ✅
- Статус: Завершен
- Зависимости: Этап 3 ✅
- Прогресс: 4.1 ✅, 4.2 ✅, 4.3 ✅, 4.4 ✅, 4.5 ✅, 4.6 ✅
- Коммиты: `4bad2ec` (4.1), `4c736d5` (4.2, 4.5), `33cf782` (4.3), `5a968de` (4.4), `87c7e38` (4.6 part 1), `[latest]` (4.6 complete)
- Дата завершения: 2024-12-25
- Результат: Все подэтапы завершены - полный функционал frontend реализован

### Следующие этапы

- Этап 3: API доступа к кабинету (Server)
- Этап 4: Frontend (Server)
- Этап 5-8: ESP32 (Controller)
- Этап 9: Тестирование
- Этап 10: Документация

---

## Известные проблемы и решения

### Проблема: Настройка SHA-512 для Ed25519

**Описание:** При первой реализации возникла проблема с настройкой SHA-512 для библиотеки `@noble/ed25519`.

**Причина:** Неправильное использование API библиотеки - попытка настройки через `etc.sha512Sync` вместо `hashes.sha512`.

**Решение:** Использован правильный способ настройки:
```typescript
ed25519Lib.hashes.sha512 = (m: Uint8Array) => sha512(m);
ed25519Lib.hashes.sha512Async = (m: Uint8Array) => Promise.resolve(sha512(m));
```

**Статус:** ✅ Решено в коммите `74897df`

---

**Дата создания:** 2024-12-19  
**Дата обновления:** 2024-12-25  
**Версия:** 2.4  
**Статус:** Этапы 1-4 завершены (Server), переход к Этапу 5 (ESP32)

